# Architecture decision records

One file per decision, `NNNN-slug.md`: a title plus a few sentences of context, decision, and why (optional Alternatives / Consequences when they add value). Record a decision only when all three gates hold: it is hard to reverse, it would be surprising without context, and it was a real trade-off between genuine alternatives.
