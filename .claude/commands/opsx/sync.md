---
name: "OPSX: Sync"
description: Sync delta specs from a change to main specs
category: Workflow
tags: [workflow, specs, experimental]
---

Use the Skill tool to invoke `openspec-sync-specs` with args: "$ARGUMENTS".

This command is a thin alias; the skill (`.claude/skills/openspec-sync-specs/SKILL.md`) is the single source of truth for the workflow.
