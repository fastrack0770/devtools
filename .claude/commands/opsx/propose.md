---
name: "OPSX: Propose"
description: Propose a new change - create it and generate all artifacts in one step
category: Workflow
tags: [workflow, artifacts, experimental]
---

Use the Skill tool to invoke `openspec-propose` with args: "$ARGUMENTS".

This command is a thin alias; the skill (`.claude/skills/openspec-propose/SKILL.md`) is the single source of truth for the workflow.
